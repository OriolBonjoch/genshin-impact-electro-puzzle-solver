const containerBase = {
  width: '100%',
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'flex-start',
  position: 'relative',
};

const iconBase = {
  height: '100%',
  width: '100%',
  position: 'absolute',
  bottom: '0',
};

const button = {
  height: '100%',
  width: '100%',
  position: 'absolute',
  color: 'transparent',
};

const styles = {
  containerBase,
  iconBase,
  button,
};

export default styles;
