const container = {
  flex: '1 1 auto',
  height: '100%',
  display: 'flex',
  flexDirection: 'column',
};

const control = {
  m: 2,
};

const styles = {
  container,
  control,
};

export default styles;
